# 实验报告：Ollama 模型下载、调用与对外服务

## 一、实验目的

1. 掌握本地大模型运行环境 Ollama 的安装与模型下载方法。
2. 掌握通过命令行 / 接口对大模型进行调用与对话。
3. 理解「模型即服务」思想，将本地大模型封装为对外的 HTTP 服务，
   使其它程序或设备可通过网络调用本地模型能力。

## 二、实验环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 11 |
| 处理器 / 内存 | 16 GB 内存 |
| 显卡 | NVIDIA GeForce RTX 5060 Laptop（8 GB 显存） |
| 运行环境 | Python 3.13 |
| 大模型框架 | Ollama |
| 选用模型 | qwen3:8b（约 5 GB，适配 8 GB 显存） |

> 说明：本项目后端仅使用 Python 标准库（`http.server` / `urllib` / `subprocess`），
> 不依赖任何第三方包，便于在任何装有 Python 与 Ollama 的机器上直接运行。

## 三、实验内容与步骤

### 1. 下载模型

Ollama 安装完成后，模型托管在 `registry.ollama.com`，可直接拉取：

```powershell
ollama pull qwen3:8b
```

本项目同时封装了网页版下载入口（`POST /pull`），内部执行 `ollama pull`，
并以流式方式回传下载进度，方便在界面上观察。

### 2. 调用模型

命令行直接对话：

```powershell
ollama run qwen3:8b
```

本项目封装了 `POST /chat` 接口，将请求转发到 Ollama 的 `/api/chat`，
并以服务器推送（SSE）方式流式返回生成内容。网页聊天界面即基于该接口实现。

### 3. 对外（HTTP）服务

启动本项目后端：

```powershell
python app.py
```

后端在 `0.0.0.0:5000` 监听，对外提供：

- 网页聊天界面：`http://<本机IP>:5000`
- 模型列表接口：`GET /models`
- 对话接口：`POST /chat`
- 下载接口：`POST /pull`

为验证「对外服务」可被第三方程序调用，本项目提供 `test_client.py`，
模拟一个独立程序通过 HTTP 访问本地模型：

```powershell
python test_client.py --prompt "用一句话介绍你自己"
```

若正确返回模型生成的文本，则说明本地模型已成功作为对外服务对外提供能力。

## 四、实验结果

1. `ollama pull qwen3:8b` 执行成功，模型下载至本地。
2. 网页与命令行均可正常与模型对话，回复内容准确、流畅。
3. `test_client.py` 通过 HTTP 调用 `/chat` 接口，成功获取模型流式输出，
   证明模型已被封装为可被外部程序访问的 REST 服务。
4. 设置 `OLLAMA_HOST=0.0.0.0` 后，局域网内其它设备可通过
   `http://<本机IP>:5000` 访问该模型服务。

## 五、核心代码说明

- `app.py`：基于 `ThreadingHTTPServer` 实现多线程 HTTP 服务。
  - `do_GET`：返回网页界面或模型列表；
  - `_handle_pull`：以子进程调用 `ollama pull` 并流式转发日志（SSE）；
  - `_handle_chat`：将对话请求转发至 Ollama `/api/chat` 并流式转发结果（SSE）。
- `static/index.html`：前端聊天界面，包含「下载模型」「对话调用」两大区域。
- `test_client.py`：第三方调用示例，演示外部程序如何使用对外服务。

## 六、实验总结

通过本次实验，完成了「模型下载 → 模型调用 → 对外服务」的完整链路，
理解了本地大模型如何被封装为网络服务并供多端复用，为后续在应用中
集成本地 AI 能力（如移动端 App 调用局域网模型）打下基础。
